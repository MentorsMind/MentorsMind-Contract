// Stub file for generated token contract bindings
// Run `pnpm run generate:token` to generate actual bindings

export class Client {
  constructor(address: string) {
    this.address = address;
  }
  address: string;
}

export * from './types.js';
