// Stub file for generated session_registry contract bindings
// Run `pnpm run generate:session_registry` to generate actual bindings

export class Client {
  constructor(address: string) {
    this.address = address;
  }
  address: string;
}

export * from './types.js';
