// Stub file for generated reputation contract bindings
// Run `pnpm run generate:reputation` to generate actual bindings

export class Client {
  constructor(address: string) {
    this.address = address;
  }
  address: string;
}

export * from './types.js';
